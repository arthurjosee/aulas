#include <stdio.h> 

int main() {
	int x;
	scanf ("%i", &x);
	while (x==0){
		printf ("Erro\n");
		scanf ("%i", &x);
		
// n-- (decrementa de 1 em 1 a variavel n)
// n++ (aumenta de 1 em 1 a variavel n)
// n+=x "ex: n+=3" (aumenta x vezes a variavel n)

	}
	printf ("Agora sim");
return 0;
}

